<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav"
    app:startDestination="@id/kodBelirleme">

    <fragment
        android:id="@+id/anasayfa"
        android:name="com.example.socketchatapp.Anasayfa"
        android:label="fragment_anasayfa"
        tools:layout="@layout/fragment_anasayfa" />
    <fragment
        android:id="@+id/kodBelirleme"
        android:name="com.example.socketchatapp.KodBelirleme"
        android:label="fragment_kod_belirleme"
        tools:layout="@layout/fragment_kod_belirleme" >
        <action
            android:id="@+id/action_kodBelirleme_to_anasayfa"
            app:destination="@id/anasayfa" />
    </fragment>
</navigation>